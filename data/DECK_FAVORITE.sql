CREATE TABLE DECK_FAVORITE (
	FAV_USER_ID INT,
	DECK_USER_ID INT,
	DECK_ID VARCHAR(16),
	DELETE_FLAG TINYINT DEFAULT 0,
	ENTRY_DATE TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	PRIMARY KEY(FAV_USER_ID, DECK_USER_ID, DECK_ID)
)
