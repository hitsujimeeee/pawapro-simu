CREATE VIEW BASE_POINT_VIEW AS
SELECT
	H.TYPE,
	H.POINT,
	D.POWER,
	D.SPEED,
	D.TECH,
	D.SCREWBALL,
	D.MENTAL,
	H.ASSESSMENT
FROM
	BASE_POINT_HEADER H
LEFT JOIN
 	BASE_POINT_DETAIL D
ON
	H.TYPE = D.TYPE
AND
	H.POINT BETWEEN D.POINT_FROM AND D.POINT_TO
ORDER BY
	H.TYPE, H.POINT
