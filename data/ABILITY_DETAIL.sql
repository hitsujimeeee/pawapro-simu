CREATE TABLE ABILITY_DETAIL (
	ID VARCHAR(4) NOT NULL PRIMARY KEY,
	HEADER_ID INT,
	NAME VARCHAR(32),
	POWER INT,
	SPEED INT,
	TECH INT,
	SCREWBALL INT,
	MENTAL INT,
	UPPER VARCHAR(4),
	LOWER VARCHAR(4),
	ASSESSMENT DOUBLE,
	TYPE INT
);
