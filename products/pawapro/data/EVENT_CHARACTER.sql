CREATE TABLE EVENT_CHARACTER(
	ID VARCHAR(4) PRIMARY KEY NOT NULL,
	NAME VARCHAR(32),
	NAME_READ VARCHAR(32),
	PARENT_ID VARCHAR(4),
	CHARA_TYPE VARCHAR(8),
	TRAINING_TYPE VARCHAR(8),
	EVENT_TYPE INT
);
